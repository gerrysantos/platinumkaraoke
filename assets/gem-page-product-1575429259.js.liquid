{% comment %}
	GEMPAGE BUILDER (https://apps.shopify.com/gempage)

	You SHOULD NOT modify source code in this page because
	It is automatically generated from GEMPAGE BUILDER
	Try to edit page with the live editor.
{% endcomment %}

        jQuery(function() {
            var $module = jQuery('#m-1555987499962').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1555987499962-0').children('.module');
            var effect = $module.attr('data-effect');
            $module.gfV3ProductImage({
                'effect': effect
            })
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1545023608884').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var mode = jQuery('.gryffeditor').hasClass('editing') ? 'dev' : 'production';
            var $module = jQuery('#m-1567047203108').children('.module');
            if (mode == 'dev') {
                jQuery('#m-1567047203108').attr('data-name', '');
                jQuery('#m-1567047203108').css('background-image', 'none');
                jQuery('#m-1567047203108').css('min-height', '50px');
                jQuery('#m-1567047203108').removeAttr('data-image');
                
                var flag = true;
                var $bkLiquid = parent.jQuery('body').find('#gfFrame').contents().find('#module-1567047203108');
                if ($bkLiquid && $bkLiquid.length > 0) {
                    var $settings = $bkLiquid.find('.settings');
                    try {
                        var name = '';
                        var imageUrl = '';
                        settings = JSON.parse($settings.html());
                        for (var i = 0; i < settings.length; i++) {
                            if (settings[i].name == 'name') {
                                name = settings[i].default_value
                            }
                            if (settings[i].name == 'image') {
                                imageUrl = settings[i].default_value
                            }
                        }
                        if (imageUrl != '') {
                            flag = false;
                            jQuery('#m-1567047203108').css('background-image', 'url(' + imageUrl + ')');
                            jQuery('#m-1567047203108').css('min-height', '100px');
                            jQuery('#m-1567047203108').attr('data-image', 'true');
                        }
                        if (name != '' && name != 'Custom Code') {
                            flag = false;
                            jQuery('#m-1567047203108').attr('data-name', name);
                        }
                    } catch(error) {
                        console.log(error);
                    }
                }
                if (flag) {
                    jQuery('#m-1567047203108').attr('data-name', 'Right click on the module, then choose Edit Html / Liquid option to start writing your custom code.');
                }
            }

        }); 
    
        jQuery(function() {
          var $module = jQuery('#m-1547004025374').children('.module');
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1546591244715').children('.module');
            $module.gfV3Product();
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1547105251926').children('.module');
            var style = $module.attr('data-style');
            var updatePrice = $module.attr('data-updateprice');

            $module.gfV3ProductQuantity({
                'style': style,
                'updatePrice': updatePrice
            });
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1545023635368-3').children('.module');
            $module.gfV3ProductCartButton({ onItemAdded: function() {}});
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1548491527086').children('.module');
            $module.gfV3ProductPrice();
        }); 
    
(function( jQuery ){
  // var $module = jQuery('#m-1634284052803').children('.module');
  // You can add custom Javascript code right here.
})( window.GemQuery || jQuery );
    
        jQuery(function() {
            var $module = jQuery('#m-1560764989574').children('.module');
        }); 
    
        jQuery(function() {
            var $module = jQuery('#m-1560764989431').children('.module');
        }); 
    
        jQuery(function() {
            var mode = jQuery('.gryffeditor').hasClass('editing') ? 'dev' : 'production';
            var $module = jQuery('#m-1550194363298').children('.module');
            if (mode == 'dev') {
                jQuery('#m-1550194363298').attr('data-name', '');
                jQuery('#m-1550194363298').css('background-image', 'none');
                jQuery('#m-1550194363298').css('min-height', '50px');
                jQuery('#m-1550194363298').removeAttr('data-image');
                
                var flag = true;
                var $bkLiquid = parent.jQuery('body').find('#gfFrame').contents().find('#module-1550194363298');
                if ($bkLiquid && $bkLiquid.length > 0) {
                    var $settings = $bkLiquid.find('.settings');
                    try {
                        var name = '';
                        var imageUrl = '';
                        settings = JSON.parse($settings.html());
                        for (var i = 0; i < settings.length; i++) {
                            if (settings[i].name == 'name') {
                                name = settings[i].default_value
                            }
                            if (settings[i].name == 'image') {
                                imageUrl = settings[i].default_value
                            }
                        }
                        if (imageUrl != '') {
                            flag = false;
                            jQuery('#m-1550194363298').css('background-image', 'url(' + imageUrl + ')');
                            jQuery('#m-1550194363298').css('min-height', '100px');
                            jQuery('#m-1550194363298').attr('data-image', 'true');
                        }
                        if (name != '' && name != 'Custom Code') {
                            flag = false;
                            jQuery('#m-1550194363298').attr('data-name', name);
                        }
                    } catch(error) {
                        console.log(error);
                    }
                }
                if (flag) {
                    jQuery('#m-1550194363298').attr('data-name', 'Right click on the module, then choose Edit Html / Liquid option to start writing your custom code.');
                }
            }

        }); 
    
    jQuery(function() {
        var $module = jQuery('#m-1575429851604').children('.module');
            $module.gfV1Popup();

        var mode = jQuery('.gryffeditor').hasClass('editing') ? 'dev' : 'production';

        if(mode == 'dev') {
            var moduleId = "1575429851604";
            var moduleIdSlug = moduleId.toString().replace(/-/g, '');
            if (moduleIdSlug == "1575429851604") {
                window.getPopup1575429851604 = function() {
                    return $module.data('gfv1popup');
                }
            }
        }
    });
  
                          (function( jQuery ){
                              try{
                                  if(jQuery().gfV2Lazyload){
                                      var $module = jQuery(window);
                                      $module.gfV2Lazyload({delay:125})
                                  }
                              }catch(err){
                                  console.error(err)
                              }
                          })( window.GemQuery || jQuery );
                      